import HomeHeader from "../../components/HomeHeader";
import RestaurantList from "../../components/RestaurantList";

const Home = () => (
  <>
    <HomeHeader />
    <RestaurantList />
  </>
);

export default Home;
